# 像素铸造 IDE（PixelFoundry IDE）项目规划（ROADMAP）

> 目标读者：维护者与潜在贡献者。本文档是项目发展的"活文档"，随实现持续更新。
> English version: [ROADMAP.md](ROADMAP.md)。

## 1. 愿景

把「AI 生成 → 像素化 → 精修 → 游戏素材」全流程做成**像素优先的一站式工具**：

- 输入：文本描述 / 自备参考图 / 图生视频
- 产出：像素铸造（GIF/APNG/PNG 序列）、精灵图、**地图瓦片与瓦片地图**、可被任意游戏引擎直接使用的素材

## 2. 指导原则

1. **像素优先**：任何处理（缩放 / 键控 / 聚类 / 无缝化）都保证像素锐利、不模糊——"Perfect Pixel" 是本项目的品牌承诺。
2. **可离线演示**：Mock API 永远能跑通全流程，新用户零成本上手。
3. **服务商无关**：API 适配层隔离，新服务商只加配置不改代码。
4. **质量 > 数量**：每个功能做到"像素级完成度"，不堆半成品。
5. **可持续维护**：测试、CI、i18n、文档与功能同步演进。

## 3. 当前状态（v1.1.0）

| 模块 | 状态 |
|------|------|
| Solo 一键流程（文生图→图生视频→像素化→抠图→导出） | ✅ 可用（含参考图 i2i、循环闭合、背景稳定、多服务商适配、尺寸自动回退） |
| IDE 分步工作区（6 步、时间轴、分步参数面板） | ✅ 可用（v1.1 重排为「顶部步骤条 + 三栏」，首帧/帧序列打通） |
| 像素编辑器（形状/对称/环绕/变换 + 选择/图层、色族调色板、取色圆盘、导入导出） | ✅ 可用 |
| 精灵图（文生图整张网格 → 裁切 → 抠图 → 导出，同步 IDE） | ✅ 可用 |
| 独立像素板块（分辨率设置、双向同步、图生视频首帧） | ✅ 可用（v1.1 资源栏内可拖动、筛选行合并、右栏精简） |
| **中转站 API 接入（鉴权 / 端点 / 字段路径 / curl 导入 / 一键适配）** | ✅ 可用（v1.1 主线，见下） |
| **瓦片地图模式（第 5 模式，v0.2–v0.3 主线）** | ✅ 可用 |
| **Krita 式界面（菜单栏 / 上下文工具条 / docker 停靠面板 / 可调宽分栏 / 堆叠填充）** | ✅ 可用（全 5 个模式） |
| 中英 i18n + UI 缩放 + DSH 风格图标 | ✅ 可用（中英往返无残留） |
| CI（GitHub Actions，Py3.11/3.13 × Win/Linux），**700** 项测试 | ✅ 运行中 |
| Windows 打包（PyInstaller onedir）与 GitHub Release | ✅ v1.1.0 |

### v1.1.0 主要变化

- **中转站 API 一键适配**：`core/api/endpoint_probe.py` 探测常见视频提交端点（默认只发无害 GET），
  Base URL 自动规整（粘贴完整端点地址也能拆对），结果表一键写回提交/轮询端点与服务商适配器；
  另有 6 种鉴权方式、15 个模板占位符、可配提交方法与轮询请求体、字段路径候选回退，
  以及「预览请求 / 测试并检测字段 / 从 curl 导入」。详见 `docs/api_setup.md`。
- **IDE 模式重排**：顶部步骤条（✓ 已完成 / ▶ 当前 / ○ 未开始，可点切换）+ 三栏（资源 | 预览·编辑·提示词 + 时间轴 |
  参数 + 日志），参数栏默认展开；**修掉两个卡流程 bug**（生图后无法加入帧序列、首帧生成视频卡住）。
- **独立画布布局**：资源栏内部（包列表 / 目录树 / 缩略图）分隔条可拖动，去掉写死高度上限；
  分类下拉与搜索合并成一行；右栏由三块精简为两块。
- **质量**：中英 i18n 补齐约 70 条并修掉 3 处真实缺陷；测试 600 → 700；CI 四平台全绿。

### v1.0.0 主要变化（历史）

- **界面大改（参考 Krita）**：菜单栏 + 上下文工具条 + 模式栏 + 状态栏；统一的 docker 停靠面板
  （可折叠、整栏收起成竖标签、拖拽调宽并记忆、**折叠即让位**给同列展开面板）；5 个模式的参数栏
  全部改为可拖拽分栏；深/浅色 QSS 重写。
- **像素编辑器实用功能**：直线/矩形/椭圆形状工具（拖动预览）、对称绘制（左右/上下/四象限）、
  环绕绘制（无缝瓦片）、画布变换（翻转/旋转/裁剪/尺寸/整数缩放）、导出倍率 1×–8×、
  复制到剪贴板、GIMP `.gpl` 调色板导入导出、工具快捷键。
- **瓦片包逐级目录浏览**：包内 `atlas/`、`tiles/<地形>/`、`textures/`、`source/`、`pieces/`、
  `props/` 每一级都能浏览并送入画布；支持没有 manifest 的普通图片文件夹。
- **质量**：中英双向切换不再残留；地图瓦片缓存按对象身份校验（消除偶发串图）；
  删除死代码/重复实现（英文包键 1028→977，重复实现合并为共用模块），测试 527 → 600 项。

### 瓦片地图模式（已完成）

- **地块生态**：一句提示词 → 2×2×3 底图（基础地形 + 3 特征）；自动去格线、检文字（笔画宽度签名）、
  九宫格中值纹理去一次性文字；纹理偏移错位缝合保证**逐像素首尾相等**；**对齐式构图**按掩码派生整族瓦片，
  相邻瓦片共享边逐像素一致；条带深度/分层描边/倒角/环境阴影由 AI 底图**实测**；
  非内部边缘向内不规则噪声；**跨地形/跨瓦片包的块状渗透融合**消除硬边。
- **建筑类**：墙体 **16-tile 族**（直墙/转角/内外角/T 形/十字/端头/孤立块），固定截面几何、
  外部透明可叠加地块、程序化 1px 描边、顶面 + 立面明暗、AI 推导门与立柱件；地图视图带
  **实时自动拼接墙层**（涂/擦即自动选型）。
- **素材（道具）**：变体网格生成 → 按提示词判断主体明暗选择**纯白/纯黑底**并直接按范围删像素 +
  边缘洪泛抠底 → alpha 硬化 0/255 → 裁紧、底部对齐；放置可缩放（25%–400%）。
- **瓦片包与导出**：完整瓦片集目录 + zip（47 图集 8×6、逐张瓦片、
  textures/pieces/props、全部元信息、README）；导入支持文件夹 / zip / `.tilepack`。
- **地图预览**：**无需先生成**即可进入；多包混铺（地块/建筑/素材）、网格开关、Ctrl+左键平移、
  滚轮缩放、画笔/橡皮/旋转/缩放；**柏林噪声大地图**（≤400×400，可散布建筑，瓦片合成按掩码缓存）。
- **俯视 2.5D**：顶面层（47-tile 族）+ 崖壁层（16-tile 族，由地形艺术推导）；预览可抬高/降低格子，
  长崖壁逐格连续、两端自动收口。
- **参考图 i2i**：瓦片底图支持参考图输入，保持整套瓦片配色/风格一致。
- **布局兼容**：47-tile（8×6 blob 掩码约定）、16-tile 墙体族、**双网格**（dual grid，生成与预览；
  建筑侧为 4 区块墙体族 + 实心件）。
- 64 条核心算法约束以测试固化（共享边逐像素一致、无文字/格线残留、47 类覆盖、透明外部、
  抠底零残留、确定性可复现等）。

已知技术债：打包 onedir 体积大（~250MB）、部分工作流日志未 i18n、GUI 细节测试覆盖不全、
Tiled `.tmx/.tsx` 导入导出未做、瓦片地图图层栈仅“地块 + overlay + 墙体层”三层。

## 4. 阶段规划

### 阶段 A：地图瓦片生成 + 瓦片地图编辑器（v0.2–v0.3 已完成主体 ✅）

**A1 瓦片集生成** ✅ 已完成
- 文生图生成 2×2×3 地块生态底图（基础地形 + 3 特征）与 2×2 建筑底图、素材变体网格，提示词内置
  等大格、纯色底、**可无缝平铺**、风格一致、无文字/边框/格线约束；
- 去格线（期望位置 ± 容差最暗游程 + 残线扫描）、文字检测与修补、偏移错位缝合（wrap-equal）、
  九宫格中值纹理；对齐式构图派生整族瓦片（条带/描边/倒角/阴影实测）；
- 导出 **完整瓦片集目录 + zip**（47 图集 8×6、逐张瓦片、元信息），并兼容 Tiled 常用布局。
- DoD 达成：默认参数下相邻瓦片共享边逐像素一致，长条/长墙无每格接缝。

**A2 瓦片地图编辑器** ✅ 已完成（第 5 模式）
- 瓦片网格画布 + 多包混铺（地块/建筑/素材）、画笔/橡皮/旋转/缩放、网格开关、Ctrl+左键平移；
- **墙体层实时自动拼接**（16-tile 按四邻域选型）、建筑 overlay 层、透明外部叠加；
- **柏林噪声大地图**预览（程序化大陆/河流/山地，可散布建筑）、**双网格**渲染；
- 导出 PNG 预览 + 地图 JSON（overlay 名称/旋转/缩放、墙体层、dual 标记）。
- 待办：Tiled `.tmx/.tsx` 导入导出、洪水填充/矩形/多图层栈。

**A3 与现有管线打通** ✅ 基本完成
- 瓦片包（目录/zip）可在任意预览加载并混铺；地块/建筑/素材三类共用一套包格式；
- 待办：瓦片/地图与 IDE、像素模式的互推（精修回写）。

**A4 大图性能** 🚧 部分完成
- 已做：瓦片合成按（地形艺术 + 掩码）缓存，96×64 大地图秒级出图；跨包融合与墙体层均为向量化实现；
- 待办：视口化渲染、超大图异步导出、瓦片集懒加载。

### 阶段 B：Solo 性能与生成质量优化

**B1 性能**
- **结果缓存**：提示词 / 图片按内容哈希缓存，重复生成直接复用（省 token、省时）；
- **像素化并行**：numpy 向量化 + 多帧线程池/进程池；
- **减 token**：LLM 模板压缩（已有 max_tokens 800）、首帧 min/max side 已有，图片请求尺寸按需降档；
- 大图预览 / 导出异步化，进度粒度细化。

**B2 质量**
- **多候选生成**：`n=2~4` 生多张，按客观评分（清晰度 / 网格纯度 / 主体完整度）自动选优，UI 允许用户挑选；
- **提示词模板升级**：few-shot 示例 + 参数化风格预设（像素风 / 色板 / 描边强度）；
- **视频一致性**：抽帧后逐帧颜色直方图匹配、更好的循环首尾检测；
- **抠图升级**：可选描边处理、边缘抗锯齿、羽化增强；
- **seed 控制**：可复现生成。

**B3 质量回归基准**
- 建立示例图集 + 客观评分脚本，任何改动跑基准防质量回退（并入 CI 可选 job）。

### 阶段 C：像素编辑器 & 精灵图精进

**C1 像素编辑器**
- 更多工具：直线 / 矩形 / 椭圆 / 对称绘制 / 魔棒选择 / 像素字体文本；
- **真实图层栈**（替代当前单浮动层）+ 图层面板；
- 编辑页内嵌动画预览（帧序列直接播放）、洋葱皮强度调节；
- 快捷键自定义、图案笔刷。

**C2 精灵图**
- **页内逐帧编辑**（点格子直接进编辑器，无需先同步 IDE）；
- 裁切优化：内容包围盒 + 留白参数化、去边框参数 UI；
- 多动作精灵图（一个 sheet 多行动作）、碰撞盒标注；
- 生成结果多候选挑选。

### 阶段 D：持续打磨（贯穿始终）

- **D1 工程**：PyInstaller onefile + 图标 + 版本自检；自动更新（可选）；CI 加打包 job 与 codecov；
- **D2 体验**：空状态引导、快捷键帮助、设置项精简、错误提示友好化、日志分级与搜索；
- **D3 国际化**：英文文案 100% 覆盖、日语等更多语言包、字体适配；
- **D4 文档**：用户手册（中英）、示例画廊、API 适配文档、FAQ；
- **D5 社区**：Issues 模板、Contributing、首个外部贡献。

## 5. 新增规划（阶段 E–I，v1.1 → v2.0）

> 阶段 A–D 为 v0.2–v1.0 期间制定的规划，保留原文以便追溯；下面 E–I 是 v1.0.0 发布后按维护者意图追加的
> 方向，**不回改上面的历史内容**。五个方向的关系：E 与 G 各是一个新引擎，F 与 I 是对现有链路的深化，
> H 是横切所有生图链路的能力层。

### 阶段 E：严格等距（棱形）瓦片地图

**目标**：在现有俯视 / 俯视 2.5D 之外，新增**严格的等距（isometric，2:1 菱形）瓦片**体系 —— 从生成、掩码派生、
预览编辑到导出全链路，而不是把俯视瓦片斜着贴上去。

**E1 几何与坐标层**（新模块 `core/tilemap/isometric.py`）
- 菱形（2:1 与可配置比例）几何：格 ↔ 屏幕坐标互转 `x=(c−r)·tw/2, y=(c+r)·th/2`，以及反变换命中测试；
- **深度排序**：按 `c+r` 分层绘制（画家算法），支持同格多层（地面 / 装饰 / 建筑 / 单位）与"半格偏移"物件的排序修正；
- 等距四邻（菱形边）+ 四对角语义重定义：等距下"角"是可通行性交界，掩码家族需要独立定义。

**E2 掩码与派生**（复用对齐式构图，重写掩码层）
- 等距 **47 变体**：菱形边的直段 / 内外角 / 端头 + 对角过渡（斜向连通）；
- **斜坡与台阶族**：8 方向 ramp（低→高）、台阶、崖壁侧面的等距变体；
- 高度层：多级高台（0–N），每级顶面菱形 + 左右两侧崖面 + 与邻居的高差自动选件；与现有俯视崖壁 16-tile 共用艺术推导，
  但几何按等距重算（侧壁 1:1 像素，不随投影拉伸，保持像素锐利）；
- 复用已有：边缘向内噪声、跨地形/跨包**块状渗透融合**、共享边逐像素一致约束（等距下共享边是菱形的两条斜边）。

**E3 生成链路适配**
- 提示词模板新增等距变体（明确"等距 2:1 菱形、俯视 30°、无透视畸变"）；
- 后处理：按菱形网格做格线抹除与掩码裁切（现有 2×2×3 底图流程保留，仅替换裁切与派生几何）；
- 瓦片包元信息新增 `projection: "isometric"` 与比例字段，导出等距图集（菱形排布，含 2:1 与 1:1 两种常用排布）。

**E4 预览与编辑**
- 等距画笔（菱形命中 + 邻域自动拼接）、高度笔刷（抬升/降低/平滑）、ramp 笔刷；
- 网格/坐标显示（格坐标 + 世界坐标）、参考线（等距轴）、拾取器（点选格子取当前值）；
- 柏林噪声大地图在等距下的映射（程序化地形 → 等距高度图，河/山/海岸自动生成 ramp 与崖壁）。

**E5 导出与互通**
- 导出：等距 atlas PNG + JSON、Tiled 地图 `orientation="isometric"`（`.tmx` + `.tsx`）、
  Godot 4 `TileSet`（等距）与 Unity 等距 Tilemap 的元信息建议；
- 与像素画布互通：地图里"取一块"进画布精修后回写（复用阶段 A3 的待办）。

**验收（DoD）**
- 等距瓦片共享斜边逐像素一致；同一地图数据在俯视与等距两种视图下**拓扑一致**；
- 导出的 `.tmx/.tsx` 能在 Tiled 打开且图层/图集/属性完整；多级高台的崖壁在任意高度组合下不穿帮。

### 阶段 F：图转视频 → 序列帧算法精调

**目标**：把"AI 视频 → 抽帧 → 像素序列帧"从"能跑通"提升到"稳定、可循环、少闪烁、帧数合理"，
产出可以直接进游戏的动画。

**F1 抽帧策略**
- 内容感知抽帧：帧间相似度（感知哈希 / 颜色直方图 / 结构相似度）+ 运动能量峰值检测，保证动作关键帧不被抽掉；
- 均匀采样与目标帧数约束并存（`max_frames` / 最小帧间隔），提供"保动作"与"保时长"两种模式；
- 关键帧保护 + 首尾帧强制包含；抽帧结果可视化（时间轴高亮保留帧）。

**F2 稳定性（去抖与对齐）**
- 全局运动估计：逐帧平移/缩放/旋转（相位相关 + 特征点回退），把主体对齐回参考帧；
- 背景层估计：多帧中值/分位数合成稳定背景，抑制 AI 视频的"背景沸腾"；
- 主体分割（可选）：用抠图链路分离主体，主体对齐 + 背景固定后再合成。

**F3 循环与节奏**
- 循环闭合：首尾相似度检测 → 自动裁掉冗余尾帧或做插值回接；可选交叉淡化；
- 节奏重采样：按动作幅度自适应帧数（运动大 → 多帧）、导出 GIF 时生成**每帧延迟表**（变时长动画）；
- 循环质量指标：闭合误差、速度连续性（避免循环点"卡顿"）。

**F4 颜色一致性与像素化衔接**
- 逐帧颜色稳定：以全片中位调色板为基准做直方图/调色板匹配，消除明暗与色相漂移；
- **网格漂移检测**：现有"首帧定网格"在全片共用；新增检测（若后段网格明显漂移则分段重定网格并对齐），
  避免"完美像素"在长视频上越走越歪；
- 像素化顺序固定为"先稳定 → 再量化 → 再抠图"，避免逐帧独立处理引入抖动。

**F5 质量评估与建议**
- 客观指标：闪烁指数、主体抖动幅度、循环误差、帧间颜色漂移、透明边缘稳定性；
- UI 报告：给出"哪个指标不达标 + 建议怎么改"（增加帧数 / 提高倍速 / 换抽帧模式 / 重试）。

**验收（DoD）**
- 循环 GIF 首尾无缝、无可见明暗跳变；同一段视频用默认参数两次运行结果**逐帧一致**（确定性）；
- 指标面板能正确指出劣化类型；抽帧结果与像素化网格一致（不出现逐帧抖动）。

### 阶段 G：骨骼 + 矢量方法生成序列帧

**目标**：**不依赖视频 API**，从单张像素原图用「骨骼绑定 + 网格形变」程序化烘焙出动画序列帧
（剪纸/皮影式 2D 骨骼动画），并且**保持像素风格不退化成平滑插值图**。

**G1 绑定编辑器**（画布内的新模式）
- 骨骼：关节树（位置/旋转/缩放/父子关系/IK 可选），直接画在像素画布上，可视化骨架叠加层；
- 网格与权重：自动把主体切成三角/四边形网格并计算权重（距离衰减 + alpha 边缘约束 + 关节处加密），
  支持手动调整权重笔刷（加/减/平滑）；
- 分层：主体按色族或手绘遮罩分成"躯干/手臂/腿/武器"等部件，部件可独立绑定与排序（避免遮挡错乱）。

**G2 像素保真的形变**
- 形变：线性混合蒙皮（LBS）+ 薄板样条（TPS）备用；**整数位移 + 最近邻重采样**，禁止双线性带来的半透明边；
- 颜色约束：形变后回到原调色板（量化 + 色族映射），保证颜色数不增加、渐变不断裂；
- 关节处理：关节处局部重采样 / 局部旋转 / 像素级修补（可选"自动补洞"与"手工修帧"）；
- 抗抖动：亚像素运动量化为整数步进 + 相位抖动抑制（避免"呼吸"式抖动）。

**G3 动作与烘焙**
- 预设动作库：走路 / 跑步 / 待机 / 跳跃 / 挥砍 / 受击 / 死亡等，关键帧 + 插值曲线（缓入缓出），
  参数化周期、幅度、相位、镜像；
- 时间轴烘焙：按目标 FPS/帧数烘焙成序列帧 → 复用现有像素化 / 抠图 / 导出（GIF / APNG / PNG / 精灵图 / 多动作 sheet）；
- 换肤复用：同一套骨骼 + 动作可套用到不同角色图（同尺寸/同结构），批量出素材。

**G4 与 AI 链路结合**
- AI 视频抽帧（阶段 F）作为**运动参考**：从视频估计关节点轨迹 → 驱动骨骼 → 本地烘焙出干净帧（结构稳定 + 无 AI 抖动）；
- 反向：骨骼烘焙出的帧可作为 i2i 参考，让 AI 做风格化重绘后再回像素。

**G5 导出与互通**
- 自研骨骼 + 动作 JSON（可版本化、可 diff），另提供 Spine / DragonBones 兼容 JSON 导出供引擎二次利用；
- 导出预览图（骨架 + 关键帧网格）进瓦片包 / 项目文件。

**验收（DoD）**
- 一张 32×32 角色图 → 8 帧走路动画：无插值污染（无半透明边）、颜色数不增加、边缘硬；
- 同一动作套用到第二张同规格角色图可直接出帧；烘焙结果确定性可复现。

**风险与备选**：像素级形变的"关节撕裂/次像素抖动"是主要难点 —— 备选方案是"部件分离 + 整数像素位移 + 手工修帧"
（下策但绝对保像素），并把自动方案作为加速手段。

### 阶段 H：风格插件（LoRA 类风格控制）

**目标**：把"风格"从散落的提示词里抽出来，做成**可安装、可组合、可分享**的插件，并在所有生图链路上统一生效。

**H1 插件格式（声明式，不执行代码）**
- 一个目录或 zip：`style.json`（`id / name / version / author / tags / targets / license`）+
  正向提示词片段 + 负向词 + 质量词 + 参考图（1–N 张）+ 调色板（可选 `.gpl`）+ 预览图；
- 校验与版本兼容：字段白名单 + 版本号，加载失败给出可读原因；插件只做数据，**不允许脚本**（安全边界）。

**H2 生效分层（按服务商能力自动降级）**
1. **提示词层**（所有服务商）：风格片段 + 负向词 + 质量词拼装，支持权重语法；
2. **参考图层**：自动把插件参考图喂给已有 i2i 链路（Solo / 精灵图 / 瓦片集）；
3. **调色板层**：生成后按插件调色板量化 / 色族映射（像素优先，落到现有调色板链路）；
4. **原生 LoRA 层**：对自托管后端（ComfyUI / SD WebUI / 兼容 API）通过现有**自定义请求体模板**传
   `lora_name` / `lora_weight` / `model` / `vae` / `sampler` 等字段；提供 ComfyUI 工作流模板示例。

**H3 组合与权重**
- 多个风格插件可叠加，每个 0–150% 权重；冲突检测（互斥标签、调色板冲突、重复负向词）并给出提示；
- 预设组合：把"插件组合 + 权重 + 参数"存成可命名的风格预设，一键切换。

**H4 管理 UI 与分发**
- 「设置 → 风格」与主界面工具条：本地插件列表（导入 zip / 打开目录 / 启用禁用 / 预览缩略图 / 删除），
  当前生效插件与权重可视化；
- 从项目内导出插件（把当前提示词 + 参考图 + 调色板打包成插件）；导入时展示作者/许可/预览；
- 统一的 `StyleContext` 贯穿文生图、精灵图、瓦片集，保证同一插件在三处风格一致。

**验收（DoD）**
- 导入一个像素风插件后，Solo / 精灵图 / 瓦片集三处产出的风格一致；
- 插件在无 LoRA 能力的服务商上自动降级到"提示词 + 参考图 + 调色板"且仍有明显风格效果；
- 插件可一键导出并在他机导入复现（含预览图与许可信息）。

### 阶段 I：独立像素画布生产力提升

**目标**：让独立像素画布能"独立干完一天的活"——不切到 IDE 模式也能完成角色/道具/图标的完整制作。

**I1 图层与选区**（编辑器核心升级）
- 真实图层栈：多层 + 图层组 + 混合模式（正常/正片叠底/叠加/滤色…）+ 不透明度 + 锁定/隐藏 + 拖拽排序 + 图层面板；
- 选区增强：魔棒（容差/连续/反选）、按颜色选择、加/减/交选、选区变换（移动/旋转/翻转/缩放/斜切）、浮动选区；
- 历史增强：命名快照、历史面板（可跳回）、大操作合并（拖动只记一次）。

**I2 绘制效率工具**
- **图章与图案**：多格笔刷（一次落 2×2 / 3×3 图案）、图案填充（用包内瓦片作图章）、随机变体笔刷（树/花草/石头散布，指定密度与区域）；
- 像素字体文本：内置 5×7 / 8×8 位图字体（中英）+ 描边 / 阴影 / 间距 / 对齐；
- 抖动与渐变：Bayer / 噪声抖动填充、双色渐变、色族内渐变；
- 笔刷增强：自定义笔刷（从选区存笔刷）、笔刷预设面板、压感/速度映射（可选）。

**I3 动画工作台（画布内）**
- 多帧管理：时间轴 + 增删/复制/反转/位移帧、帧标签、循环区间；洋葱皮强度与前后帧数可调；
- 播放预览（可变速）、**帧位移批量生成**（上下浮动/呼吸/旋转等一键出帧，与阶段 G 的骨骼烘焙共用时间轴）；
- 直接导出 GIF / APNG / PNG 序列 / 精灵图（含网格排布与元信息）。

**I4 参考与量测**
- 参考图层：置底半透明、网格对齐、缩放对齐（描图用）；
- 像素尺 / 坐标拾取 / 距离测量 / 角度参考线；吸管采样历史（最近 N 色）。

**I5 与包 / 地图 / IDE 的互通**
- 画布任意区域 → 存成瓦片包素材（自动命名与元信息）；地图预览"取一块"进画布精修后回写（与 A3 待办合并）；
- 与 IDE、精灵图模式的双向同步增强（同步时保留图层结构、帧序列方向可选）。

**I6 效率与自动化**
- 命令面板（`Ctrl+Shift+P` 搜索并执行任意命令）、完整快捷键自定义、冲突检测；
- **宏录制**：把一串操作录成可重放的动作（如"描边 + 阴影 + 导出 4 尺寸"），可绑定快捷键/工具条按钮；
- 批量导出：多尺寸 / 多格式 / 多命名规则一次导出，导出预设可保存。

**验收（DoD）**
- 只用画布完成"32×32 角色 → 8 帧走路 → 精灵图 + GIF 导出"全流程；
- 图层/选区/撤销在 128×128 画布上操作流畅（无明显卡顿）；宏录制可稳定重放并导出多尺寸产物。


## 6. 里程碑

| 里程碑 | 范围 | 状态 |
|--------|------|------|
| **M1（v0.2）** | A1 瓦片集 + A2 瓦片地图 MVP + 打包 | ✅ 已发布 |
| **M2（v0.3）** | 47/16/双网格算法精修、建筑 16-tile 族、素材（道具）链路、瓦片包导入导出、交界渗透融合、柏林噪声大地图、免生成预览 + 截图画廊 | ✅ 已发布 |
| **M3** | Tiled `.tmx/.tsx` 导入导出、地图图层栈与矩形/填充工具、A3 精修回写、A4 视口化/异步导出 | 下一版本 |
| **M4** | B1 结果缓存/并行 + B2 多候选与模板升级 + C2 精灵图内联编辑 | M3 之后 |
| **M5（v1.0）** | 稳定打磨、100% i18n、D1 自动更新、社区运营、**v1.0** | ✅ 已发布（2026-09-18） |
| **M6（v1.1）** | 中转站 API 一键适配（鉴权/端点探测/字段路径/curl 导入）+ IDE 与画布布局重做 + 首帧/帧序列打通；**I 画布生产力**（图层栈/选区/图章图案/宏录制/命令面板）与 **F 抽帧精调** 仍待做 | 🚧 部分交付（v1.1.0，2026-09-20） |
| **M7（v1.2）** | **E 等距（棱形）瓦片**：几何与掩码层、斜坡/台阶族、等距预览与编辑器、等距导出（Tiled/Godot） | 规划中 |
| **M8（v1.3）** | **G 骨骼 + 矢量序列帧**：绑定编辑器、像素保真形变、预设动作库与烘焙、与 AI 抽帧互补 | 规划中 |
| **M9（v2.0）** | **H 风格插件（LoRA 类）**：插件格式与分层生效、组合权重、管理 UI、跨链路一致；配套 D1–D5 打磨 | 规划中（大版本） |

> 排期思路：I/F 是现有链路的深化，改动面可控、对日常使用收益最直接，先做；E 是新引擎但复用现有瓦片算法；
> G 是最重的**新引擎**（需要新的绑定/形变数据模型与像素保真约束）；H 是横切能力层，放在动画与投影体系稳定之后做，
> 避免同时改动生图链路与渲染链路。

## 7. 优先建议

**既有清单（阶段 A–D，仍然有效）**

1. Tiled `.tmx/.tsx` 导入导出——让导出物直接进入主流关卡编辑器（**并到 E5 一起做等距变体**）；
2. 地图图层栈与矩形/填充/魔棒——地图编辑从“预览”走向“可用编辑器”；
3. Solo 结果缓存与像素化并行——用户最直接的省时省钱；
4. 编辑页动画预览 + 洋葱皮强度——编辑器日常体验提升（**并入 I3**）；
5. 多候选生图——质量感知最大提升点。

**新增清单（阶段 E–I，按收益/成本排序）**

6. **F2/F4 稳定性与网格漂移检测** —— 直接决定"AI 视频 → 序列帧"能不能用，优先级最高；
7. **I1 图层栈 + I2 图案/图章笔刷** —— 像素画布从"能做"变"好用"；
8. **I6 宏录制 + 批量导出** —— 素材量产效率的关键；
9. **E1/E2 等距几何与掩码层** —— 打开等距游戏（农场/战棋/RPG）市场，且复用现有算法；
10. **H1–H3 风格插件格式与分层生效** —— 先落地"提示词 + 参考图 + 调色板"三层（无需服务商支持），原生 LoRA 后置；
11. **G1/G2 绑定与像素保真形变** —— 长期投入项，先用"部件分离 + 整数位移"链路验证价值，再补自动蒙皮。

## 8. 如何跟踪

- 建议在 GitHub 建立 **Milestones（M1–M9）** 与 **Labels**：`tiles` / `map-editor` / `isometric` /
  `solo-quality` / `video-frames` / `skeleton-anim` / `style-plugin` / `performance` / `pixel-editor` /
  `sprite` / `i18n` / `packaging` / `docs` / `good-first-issue`；
- 每个 PR 关联对应 Issue，里程碑按此文档拆解；
- 质量底线：任何改动必须跑通 `pytest`（当前 **600** 项）且不引入回归；CI 四平台（Py3.11/3.13 × Win/Linux）
  必须全绿；像素相关改动需附"共享边逐像素一致 / 无插值污染"的证据（截图或测试）。

---

*最后更新：2026-09-20（随 v1.1.0 发布更新：中转站 API 一键适配、IDE 三栏重排与首帧/帧序列打通、
独立画布布局优化；阶段 E–I 规划保持不变，M6 标记为部分交付）*
